# Windows "mark of the web" metadata
*.Zone.Identifier
*:Zone.Identifier

# Playwright MCP test artifacts
.playwright-mcp/

# Unused source screenshot (not referenced by the site)
Screenshot 2026-06-04 142656.png

# OS / editor cruft
.DS_Store
Thumbs.db

# Netlify
.netlify/

# Local agent/skill tooling (not part of the site)
.agents/
skills-lock.json
.claude/

# Secrets — never commit API keys/tokens (e.g. TinyPNG/Tinify key)
.env
.env.*
*.key
secrets*

# Personal info dump — kept local only, never published
details.txt

# Logo concept candidates + comparison page — kept locally for reference,
# but only assets/logo.svg (the chosen "Terminal" logo) ships with the site.
assets/logos/
logo-preview.html
